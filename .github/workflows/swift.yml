name: Swift

on:
  push:
    branches: [ canon ]
  pull_request:
    branches: [ canon ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: swift-actions/setup-swift@v1
    - name: Build
      run: swift build -v
    - name: Run tests
      run: swift test -v
